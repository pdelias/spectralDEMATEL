% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness.R
\name{measurement_stability}
\alias{measurement_stability}
\title{Does the type survive the noise expert ratings carry?}
\usage{
measurement_stability(
  A,
  tolerance = 0.5,
  B = 200,
  seed = NULL,
  perturb = c("nonzero", "all"),
  coupling_cut = STRUCTURE_CUTS$coupling,
  hierarchy_cut = STRUCTURE_CUTS$hierarchy
)
}
\arguments{
\item{A}{A square, non-negative numeric matrix of direct influences.}

\item{tolerance}{Half-width of the perturbation, in rating-scale points.
The default 0.5 is the noise a four- or five-point expert scale is
generally taken to carry.}

\item{B}{Number of perturbed matrices to draw.}

\item{seed}{Optional integer, so a result can be reproduced.}

\item{perturb}{Which entries to disturb. \code{"nonzero"}, the default, leaves
recorded zeros alone; \code{"all"} disturbs every off-diagonal entry.}

\item{coupling_cut, hierarchy_cut}{Passed to \code{\link[=structural_type]{structural_type()}}.}
}
\value{
A list, or \code{NULL} when the matrix cannot be diagnosed:
\code{observed_type}, \code{share_same} (proportion of perturbed matrices keeping
that type), \code{type_table}, \code{B_admissible} (draws that could be diagnosed at
all), \code{tolerance} and \code{perturb}.
}
\description{
Perturbs the influence ratings by a stated tolerance and recomputes the
structural type. Expert judgements on a four- or five-point scale carry at
least half a point of noise, and a classification that does not survive that
is not a classification.
}
\section{Why recorded zeros are left alone by default}{

A rating of zero is a judgement that there is no influence between two
factors, which is a different kind of statement from a rating of one carrying
measurement error. Disturbing zeros upward also fills in the matrix, which
destroys the sparsity the connectivity checks are about. \code{perturb = "all"} is
available for anyone who disagrees, and the choice is reported back.

Perturbed values are clamped to \verb{[0, max(A)]}: a rating cannot go negative,
and nothing should exceed the top of the scale the analyst used.
}

\examples{
ms <- measurement_stability(worked_matrices$fefo_stock_control,
                            B = 40, seed = 1)
ms$observed_type
ms$share_same

}
