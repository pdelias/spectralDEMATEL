% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{assumption_checks}
\alias{assumption_checks}
\title{Check a matrix against the assumptions the diagnostics rest on}
\usage{
assumption_checks(A, type = c("A", "T"))
}
\arguments{
\item{A}{The matrix to check. Anything may be passed; a non-matrix fails the
first check rather than raising an error.}

\item{type}{Whether \code{A} is a direct influence matrix (\code{"A"}) or an
already-published total-relation matrix (\code{"T"}). Two checks do not apply to
a total-relation matrix and are reported as \code{skipped}.}
}
\value{
A data frame of class \code{spectral_checks}, one row per check, in a
fixed order, with columns:
\describe{
\item{\code{check}}{Stable identifier. Branch on this, never on \code{reason}.}
\item{\code{verdict}}{One of \code{"pass"}, \code{"warn"}, \code{"fail"}, \code{"skipped"}.}
\item{\code{reason}}{Plain-language explanation, and what to do about it.}
\item{\code{value}}{The number behind the verdict, or \code{NA} where there is
none.}
\item{\code{factors}}{List column of integer indices of the factors at fault,
empty where the check is not about particular factors.}
}
}
\description{
Every condition is returned as a row of data: a verdict, an identifier stable
enough to branch on, a reason written for a user rather than a developer, and
where possible the factors at fault. Nothing here stops, warns or prints.
}
\section{The four verdicts}{

\code{pass} and \code{fail} are the assumption holding or not. \code{warn} is the case a
binary verdict cannot express: the matrix is in scope, but it is close enough
to a boundary that the reading is worth less than it looks. Strong
connectivity is yes or no, but a graph one edge from disconnection behaves
differently from a dense one, and numerical conditioning degrades long before
any check trips.

\code{skipped} means the check could not be evaluated because something it depends
on failed first — there is no point testing connectivity on a matrix that is
not square. It is not a pass, and an interface must not render it as one.
}

\section{A failure is not a refusal}{

The engine computes diagnostics for a matrix that fails these checks, and
\code{\link[=spectral_diagnostics]{spectral_diagnostics()}} returns the checks alongside the numbers so the two
cannot be separated. One of the two matrices in \link{worked_matrices} fails
strong connectivity and its diagnostics are published. Deciding what to do
about a failure belongs to whoever is calling this.
}

\examples{
checks <- assumption_checks(worked_matrices$resilience_capabilities)
checks[checks$verdict != "pass", c("check", "verdict", "reason")]

# Which factor is at fault:
checks$factors[checks$check == "strong_connectivity"][[1]]

}
\seealso{
\code{\link[=spectral_diagnostics]{spectral_diagnostics()}}, which returns these alongside the numbers.
}
