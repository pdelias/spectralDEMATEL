% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{spectral_diagnostics}
\alias{spectral_diagnostics}
\title{Spectral diagnostics for one DEMATEL system}
\usage{
spectral_diagnostics(A, type = c("A", "T"), checks = TRUE)
}
\arguments{
\item{A}{A square numeric matrix. Either a direct influence matrix
(\code{type = "A"}, the default) or an already-published total-relation matrix
(\code{type = "T"}).}

\item{type}{Which matrix \code{A} is. \code{"A"} normalises and builds \code{T} through
\code{\link[=dematel]{dematel()}}. \code{"T"} takes \code{A} as the total-relation matrix and recovers the
spectrum of \code{D} through the inverse Moebius map \eqn{\mu = \lambda/(1 +
\lambda)}.}

\item{checks}{Whether to run \code{\link[=assumption_checks]{assumption_checks()}} and attach the result.
Defaults to \code{TRUE} and should stay that way for anything a person will
read. \code{FALSE} returns the numbers alone, or \code{NULL} if they cannot be
computed, and exists for inner loops such as \code{\link[=surrogate_ensemble]{surrogate_ensemble()}}.}
}
\value{
With \code{checks = TRUE}, a named list that always has the same shape,
whatever was passed in. With \code{checks = FALSE}, the same list without
\code{checks} and \code{engine_version}, or \code{NULL} when the matrix is inadmissible.

\describe{
\item{\code{n}}{Number of factors.}
\item{\code{mu_max}}{\strong{Coupling}, in \eqn{(0, 1)}. The spectral radius of the
normalised matrix, so the distance to criticality. Higher means more
strongly coupled.}
\item{\code{multiplier}}{Total-to-direct multiplier, \eqn{1/(1 - \mu_{max})}.
Always greater than 1.}
\item{\code{lambda_max}}{Dominant eigenvalue of the total-relation matrix.}
\item{\code{indirect_dominant}}{\code{TRUE} when indirect effects exceed direct
ones, which happens exactly when \code{lambda_max > 1}, equivalently when
\code{mu_max > 0.5}.}
\item{\code{dominance}}{\strong{Mode dominance}, \eqn{|\lambda_2|/\lambda_{max}} in
\eqn{[0, 1)}. Low means a single propagation mode; high means a second
mode competes with the first.}
\item{\code{hierarchy_sd}}{Hierarchy as the standard deviation of the
unit-norm right eigenvector. \strong{HIGH means concentrated.}}
\item{\code{hierarchy_pr}}{Hierarchy as the participation ratio divided by
\code{n}, in \eqn{(0, 1]}. \strong{LOW means concentrated} — the opposite
direction to the other two.}
\item{\code{hierarchy_gini}}{Hierarchy as the Gini coefficient of the same
vector. \strong{HIGH means concentrated.} Size-free.}
\item{\code{entry_points}}{Right eigenvector, unit 2-norm. Weights each factor
by the influence it injects: where to apply pressure.}
\item{\code{accumulation}}{Left eigenvector, unit 2-norm. Weights each factor
by what accumulates there: where effects land.}
\item{\code{ev_condition}}{Eigenvalue condition number, \eqn{1/|v'u|}. At least
1 by construction. Bounds how far the first-order sensitivity estimates
of \code{\link[=sensitivity_matrix]{sensitivity_matrix()}} can be trusted; large values mean the
derivative is locally uninformative.}
\item{\code{checks}}{The \code{\link[=assumption_checks]{assumption_checks()}} table for this matrix.}
\item{\code{engine_version}}{The package version that produced the diagnosis.}
}
}
\description{
Computes the full diagnostic set from a single influence matrix: how strongly
the system amplifies its own influence, whether one propagation pattern
dominates, how concentrated the entry points are, where influence enters and
where it accumulates, and how far a first-order sensitivity estimate can be
trusted.
}
\section{The numbers never travel without their checks}{

A diagnosis computed on a matrix that failed a check has to carry that fact
with it, so \code{checks} is part of the returned object rather than something the
caller was told separately and may not have kept. When the matrix is
inadmissible every numeric field is \code{NA} and \code{checks} says why — the shape
does not change, so a batch job over a hundred matrices gets a rectangular
result and never a failure.

\code{engine_version} is recorded because a metric definition has moved here
before. When one moves again, old diagnoses must remain interpretable rather
than silently mean something new.
}

\section{The three hierarchy readings run in different directions}{

\code{hierarchy_sd} and \code{hierarchy_gini} are high when influence enters at a few
factors. \code{hierarchy_pr} is \strong{low} when influence enters at a few factors.
This project has inverted the direction once already, in code that had been
reviewed, and the mistake produced entirely plausible output. Anything
displaying one of these must state which reading it used and which way it
runs.

\code{hierarchy_sd} is the reading the source paper's corpus analysis uses
throughout, so it is the one behind every corpus-level result including the
coupling-hierarchy trade-off.
}

\section{Mode dominance uses the largest modulus}{

\eqn{\lambda_2} is the largest-modulus eigenvalue below the dominant one, not
the second largest real part. For a non-negative \code{T} the negative end of the
spectrum usually carries the larger modulus, so taking the real part
understates the ratio nearly everywhere — while passing every plausibility
check.
}

\examples{
A <- matrix(c(0, 2, 0,
              0, 0, 3,
              4, 0, 0), 3, 3, byrow = TRUE)
d <- spectral_diagnostics(A)
d$mu_max
d$multiplier
d$indirect_dominant

# Anything that failed, and why:
subset(d$checks, verdict != "pass", c(check, verdict))

# Inadmissible input keeps the shape and explains itself:
bad <- spectral_diagnostics(matrix(2, 4, 4))
bad$mu_max
bad$checks$reason[bad$checks$check == "totals_vary"]

}
\seealso{
\code{\link[=assumption_checks]{assumption_checks()}} for the checks alone,
\code{\link[=sensitivity_matrix]{sensitivity_matrix()}} for where to intervene,
\code{\link[=surrogate_ensemble]{surrogate_ensemble()}} for a baseline to read these numbers against.
}
