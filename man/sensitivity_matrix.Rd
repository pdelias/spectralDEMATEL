% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{sensitivity_matrix}
\alias{sensitivity_matrix}
\title{Per-link sensitivity of the dominant eigenvalue}
\usage{
sensitivity_matrix(A)
}
\arguments{
\item{A}{A square, non-negative numeric matrix of direct influences.}
}
\value{
A list of three \code{n} by \code{n} matrices, or \code{NULL} when \code{\link[=dematel]{dematel()}} found
the degenerate case. Diagonals are \code{NA} throughout, since a self-loop is
not an actionable link.

\describe{
\item{\code{local}}{The effect of the entry on the propagation structure,
holding the normalising constant fixed. Always positive.}
\item{\code{normalization}}{The effect of the entry on the normalising
constant \code{s}. Non-zero only in the row or column that currently sets
\code{s}, and always negative there.}
\item{\code{total}}{Their sum, which is the quantity to rank on.}
}
}
\description{
Returns \eqn{\partial \lambda_{max} / \partial a_{ij}} for every off-diagonal
entry of the direct influence matrix, split into the two effects that
produce it.
}
\details{
This moves the question from "which factor matters" to "which relationship,
if strengthened or weakened, moves the system most", which is much closer to
what an intervention actually is. The derivative is signed: entries with a
negative derivative \emph{lower} coupling when strengthened, and are the levers
for a system someone wants to calm rather than drive.
}
\section{Why the split is not decoration}{

\code{s} is the larger of the maximum row sum and the maximum column sum.
Increasing an entry that sits in the row setting \code{s} raises \code{s} too, which
shrinks the whole normalised matrix. The two effects oppose each other, and
in that row the normalisation effect can win — so raising an influence
rating there \emph{lowers} the dominant eigenvalue. A user shown only the total
will read that as a bug. Showing the components names it as a mechanism.
}

\section{Never show this without the condition number}{

These are first-order estimates. \code{ev_condition} from
\code{\link[=spectral_diagnostics]{spectral_diagnostics()}} bounds how far they can be trusted, and large values
mean the derivative is locally uninformative. The ranking without it
misleads, so the two ship together or neither ships.
}

\examples{
A <- matrix(c(0, 2, 1,
              3, 0, 1,
              1, 2, 0), 3, 3, byrow = TRUE)
s <- sensitivity_matrix(A)

# The single most effective link to strengthen:
which(s$total == max(s$total, na.rm = TRUE), arr.ind = TRUE)

# How far to trust that: 1 is ideal, large is uninformative.
spectral_diagnostics(A)$ev_condition

}
